_Put rest layer here._
rest layer consists of:
* calling incoming SPI implementation. 

Can also contain: 
* Controllers
* Models
* Mappers might be required to convert web models to core command / queries